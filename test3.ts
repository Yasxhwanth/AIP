import 'dotenv/config'; import { PrismaPg } from '@prisma/adapter-pg'; import { Pool } from 'pg'; import { PrismaClient } from './src/generated/prisma/client'; async function main() { try { const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL } as any); const prisma = new PrismaClient({ adapter }); await prisma.entityType.findFirst({ where: { name: 'Asset' } }); } catch (err) { require('fs').writeFileSync('err3.txt', err.message); } } main();
